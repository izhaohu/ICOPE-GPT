def prompt_template(category,content,previous,next):
    if category =="body":
        #判断是否是正文
        return f"""这是一本书的其中一部分文本；正确判断这部分文本中是否存在正文；如果文本内存在正文，返回正文的部分。文本是:{content}，返回的结果是："""
    if category =="grammar":
        #判断是否是正文
        #chatgpt work
        #return f"""请根据文本的上下文内容修正文本中的语法错误；文本中第一句话和最后一句话不做语法纠正。返回处理后的文本。待处理的文本是:[{content}]，返回的结果是："""
        return f"""请根据文本的上下文内容修正文本中的语法错误，但不要对文本中的第一句和最后一句进行语法纠正。返回处理后的文本。给定的待处理文本是：[{content}]，返回的结果是："""
        #文心一言：请帮我优化提示词：去掉文本中多余的换行符，保留必要的换行符。给定的待处理文本是：[]，返回的结果是：